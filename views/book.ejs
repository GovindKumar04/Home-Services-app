<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>booking</title>
    <style>
        
    </style>
</head>
<body>
    <% if(book && book.length > 0){ %>
        <% for(let i=0;i<book.length;i++){ %>
            <div>
                <span>Order: <%= book[i].order %></span>
                <span>Price: <%= book[i].price %></span>
            </div>
        <% } %>
    <% } else { %>
        <div>
            <p>No bookings yet</p>
        </div>
    <% } %>
</body>
</html>
